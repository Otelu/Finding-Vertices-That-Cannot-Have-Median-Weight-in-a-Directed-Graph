#include <stdio.h>
#include <stdlib.h>
int nr,r,s,j,x,y,i,n,m,a[11][11];
int main()
{


 
  scanf("%d", &n);

  
  scanf("%d", &m);
  for(i=1; i<=m; i++) 
  {scanf("%d %d", &x,&y);
      
      a[x][y]=1;
      a[y][x]=-1;
      printf("\n");

  }
  printf("\n");
 

for(i=1; i<=n; i++)
{
    s=0;
    for(j=1; j<=n; j++)
        {

                if(a[i][j]>0)
                  {
                      s=s+1;
                      for(r=1; r<=n; r++) 
                        if(a[j][r]>0)
                       s=s+1;

                  if(s>n/2)
                  {
                      nr++;
                  
                    printf("\n");
                  }
                  }

         if(a[i][j]<0)
        {
            s=s+1;
            for(r=1; r<=n; r++)
              if(a[j][r]<0)
             s=s+1;

           if(s>n/2)
            {
                nr++;
               
                printf("\n");
            }

        }
        }
}
printf("%d bile nu pot avea greutatea mediana",nr);
}